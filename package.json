{
  "name": "game-of-life",
  "version": "0.1.0",
  "description": "JavaScript implementation of Conway's Game of Life",
  "main": "dist/index.js",
  "author": "MindfulMinun <importantmails4bc@gmail.com> (https://benjic.xyz)",
  "contributors": [],
  "homepage": "https://github.com/MindfulMinun/game-of-life",
  "repository": "github:MindfulMinun/game-of-life",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && babel src -d dist",
    "watch": "nodemon --watch src --exec 'npm run build'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=8.0.0 <11.0.0"
  },
  "devDependencies": {
    "http-server": "^0.12.3",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "pad-start": "^1.0.2"
  }
}
